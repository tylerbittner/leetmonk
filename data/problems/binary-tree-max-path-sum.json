{
  "id": "binary-tree-max-path-sum",
  "title": "Binary Tree Maximum Path Sum",
  "difficulty": "hard",
  "tags": [
    "tree",
    "depth-first-search",
    "dynamic-programming"
  ],
  "patterns": [
    "depth-first-search",
    "dynamic-programming",
    "tree"
  ],
  "description": "## Binary Tree Maximum Path Sum\n\nGiven a binary tree (as a level-order list), return the maximum sum of any path in the tree.\n\nA **path** is a sequence of nodes where each pair of adjacent nodes has an edge between them. A path must contain at least one node and does not need to go through the root. Values can be negative.\n\n**Constraints:**\n- The number of nodes is in the range `[1, 3 * 10^4]`.\n- `-1000 <= Node.val <= 1000`",
  "examples": [
    {
      "input": "root = [1,2,3]",
      "output": "6",
      "explanation": "The optimal path is 2→1→3 with sum 6."
    },
    {
      "input": "root = [-10,9,20,null,null,15,7]",
      "output": "42",
      "explanation": "The optimal path is 15→20→7 with sum 42."
    }
  ],
  "exampleCases": [
    {
      "input": {
        "root": [
          1,
          2,
          3
        ]
      },
      "expected": 6,
      "explanation": "Path: 2+1+3=6"
    },
    {
      "input": {
        "root": [
          -10,
          9,
          20,
          null,
          null,
          15,
          7
        ]
      },
      "expected": 42,
      "explanation": "Path: 15+20+7=42"
    },
    {
      "input": {
        "root": [
          -3
        ]
      },
      "expected": -3,
      "explanation": "Single node, must include it"
    },
    {
      "input": {
        "root": [
          1,
          -2,
          3
        ]
      },
      "expected": 4,
      "explanation": "Path 1→3 = 4 (skip -2)"
    }
  ],
  "hiddenCases": [
    {
      "input": {
        "root": [
          2,
          -1
        ]
      },
      "expected": 2,
      "explanation": "Root only is better"
    },
    {
      "input": {
        "root": [
          -1,
          -2,
          3
        ]
      },
      "expected": 3,
      "explanation": "Just the right child"
    },
    {
      "input": {
        "root": [
          5,
          4,
          8,
          11,
          null,
          13,
          4,
          7,
          2,
          null,
          null,
          null,
          1
        ]
      },
      "expected": 48,
      "explanation": "Path through node 4: 7+11+4+5+8+13=48? No: best is 7+11+4+5+8+13=48"
    },
    {
      "input": {
        "root": [
          -1,
          -2,
          -3
        ]
      },
      "expected": -1,
      "explanation": "All negative, pick largest"
    },
    {
      "input": {
        "root": [
          1,
          2,
          null,
          3,
          null,
          4
        ]
      },
      "expected": 10,
      "explanation": "Path 4+3+2+1=10"
    },
    {
      "input": {
        "root": [
          9,
          6,
          -3,
          null,
          null,
          -6,
          2,
          null,
          null,
          2,
          null,
          -6,
          -6,
          -6
        ]
      },
      "expected": 16,
      "explanation": "Best path goes through 9 and 6"
    },
    {
      "input": {
        "root": [
          0,
          -1,
          1
        ]
      },
      "expected": 1,
      "explanation": "Only right child is positive contribution"
    },
    {
      "input": {
        "root": [
          5,
          -1,
          4,
          null,
          null,
          3,
          null
        ]
      },
      "expected": 12,
      "explanation": "Path 3+4+5=12"
    }
  ],
  "starterCode": {
    "python": "from typing import List, Optional\nfrom collections import deque\n\nclass TreeNode:\n    def __init__(self, val=0, left=None, right=None):\n        self.val = val\n        self.left = left\n        self.right = right\n\ndef build_tree(nodes: List[Optional[int]]) -> Optional[TreeNode]:\n    if not nodes or nodes[0] is None:\n        return None\n    root = TreeNode(nodes[0])\n    queue = deque([root])\n    i = 1\n    while queue and i < len(nodes):\n        node = queue.popleft()\n        if i < len(nodes) and nodes[i] is not None:\n            node.left = TreeNode(nodes[i])\n            queue.append(node.left)\n        i += 1\n        if i < len(nodes) and nodes[i] is not None:\n            node.right = TreeNode(nodes[i])\n            queue.append(node.right)\n        i += 1\n    return root\n\ndef max_path_sum(root: List[Optional[int]]) -> int:\n    root = build_tree(root)\n    pass"
  },
  "functionName": "max_path_sum",
  "solutions": [
    {
      "label": "DFS with Global Max — O(n)",
      "approach": "optimal",
      "language": "python",
      "code": "from typing import List, Optional\nfrom collections import deque\n\nclass TreeNode:\n    def __init__(self, val=0, left=None, right=None):\n        self.val = val\n        self.left = left\n        self.right = right\n\ndef build_tree(nodes: List[Optional[int]]) -> Optional[TreeNode]:\n    if not nodes or nodes[0] is None:\n        return None\n    root = TreeNode(nodes[0])\n    queue = deque([root])\n    i = 1\n    while queue and i < len(nodes):\n        node = queue.popleft()\n        if i < len(nodes) and nodes[i] is not None:\n            node.left = TreeNode(nodes[i])\n            queue.append(node.left)\n        i += 1\n        if i < len(nodes) and nodes[i] is not None:\n            node.right = TreeNode(nodes[i])\n            queue.append(node.right)\n        i += 1\n    return root\n\ndef max_path_sum(root: List[Optional[int]]) -> int:\n    root = build_tree(root)\n    res = [root.val]\n\n    def dfs(node):\n        if not node:\n            return 0\n        left_gain = max(dfs(node.left), 0)\n        right_gain = max(dfs(node.right), 0)\n\n        # Update global max: path through this node using both children\n        res[0] = max(res[0], node.val + left_gain + right_gain)\n\n        # Return max gain if we continue the path upward (can only go one direction)\n        return node.val + max(left_gain, right_gain)\n\n    dfs(root)\n    return res[0]",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(h)",
      "explanation": "For each node, compute the maximum gain from going down the left and right subtrees (clamped to 0, since negative paths should be skipped). The best path **through** this node is `node.val + left_gain + right_gain` — update the global max with this. The function **returns** `node.val + max(left_gain, right_gain)` because a path continuing upward can only extend in one direction. Track the global max in a list to allow mutation inside the nested function."
    },
    {
      "label": "Brute Force — O(n²)",
      "approach": "brute-force",
      "language": "python",
      "code": "from typing import List, Optional\nfrom collections import deque\n\nclass TreeNode:\n    def __init__(self, val=0, left=None, right=None):\n        self.val = val\n        self.left = left\n        self.right = right\n\ndef build_tree(nodes: List[Optional[int]]) -> Optional[TreeNode]:\n    if not nodes or nodes[0] is None:\n        return None\n    root = TreeNode(nodes[0])\n    queue = deque([root])\n    i = 1\n    while queue and i < len(nodes):\n        node = queue.popleft()\n        if i < len(nodes) and nodes[i] is not None:\n            node.left = TreeNode(nodes[i])\n            queue.append(node.left)\n        i += 1\n        if i < len(nodes) and nodes[i] is not None:\n            node.right = TreeNode(nodes[i])\n            queue.append(node.right)\n        i += 1\n    return root\n\ndef max_path_sum(root: List[Optional[int]]) -> int:\n    root = build_tree(root)\n\n    def max_down(node):\n        \"\"\"Max gain going down from this node (must go straight down).\"\"\"\n        if not node:\n            return 0\n        return max(0, node.val + max(max_down(node.left), max_down(node.right)))\n\n    def max_through(node):\n        \"\"\"Max path sum for any path in the subtree rooted at node.\"\"\"\n        if not node:\n            return float('-inf')\n        left_gain = max(0, max_down(node.left))\n        right_gain = max(0, max_down(node.right))\n        through_here = node.val + left_gain + right_gain\n        return max(through_here, max_through(node.left), max_through(node.right))\n\n    return max_through(root)",
      "timeComplexity": "O(n²)",
      "spaceComplexity": "O(h)",
      "explanation": "For each node, compute the best path passing through it: node.val + best_down(left) + best_down(right). Take the maximum across all nodes. The helper max_down gives the maximum gain starting from a node going only downward. This is O(n²) because max_down is recomputed from scratch for each node. The optimal O(n) solution avoids this by computing gains bottom-up in a single DFS pass."
    }
  ],
  "hints": [
    "At each node, you can form a path by combining: the node's value + the best path going down its left subtree + the best path going down its right subtree.",
    "A path going upward from a node can only extend in one direction. Return node.val + max(left, right) for the parent, but update a global max with node.val + left + right."
  ],
  "lcEquivalent": "124. Binary Tree Maximum Path Sum"
}
