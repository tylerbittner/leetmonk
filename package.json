{
  "name": "leetmonk",
  "version": "1.2.1",
  "description": "Deep practice in solitude. Master algorithms without the noise.",
  "main": "out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "start": "electron-vite build && electron-vite preview",
    "preview": "electron-vite preview",
    "dist": "electron-vite build && electron-builder",
    "test": "jest --testPathPattern='tests/' --runInBand",
    "test:problems": "jest --testPathPattern='tests/problems'",
    "test:solutions": "jest --testPathPattern='tests/solutions'",
    "test:e2e": "npx electron-vite build && npx playwright test",
    "prepare": "husky",
    "test:coverage": "jest --coverage --testPathPattern=tests/",
    "test:all": "npm test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@vitejs/plugin-react": "^4.2.1",
    "@vitest/coverage-v8": "^4.1.0",
    "electron": "^40.6.1",
    "electron-builder": "^26.8.1",
    "electron-vite": "^5.0.0",
    "husky": "^9.1.7",
    "jest": "^29.7.0",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@monaco-editor/react": "^4.6.0",
    "monaco-vim": "^0.4.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-markdown": "^9.0.1",
    "react-resizable-panels": "^2.0.13",
    "react-syntax-highlighter": "^16.1.0",
    "remark-gfm": "^4.0.0",
    "ts-fsrs": "^5.2.3"
  },
  "build": {
    "appId": "com.leetmonk.app",
    "productName": "LeetMonk",
    "mac": {
      "identity": null,
      "target": "dmg"
    },
    "directories": {
      "output": "dist"
    },
    "files": [
      "out/**/*",
      "data/**/*"
    ],
    "extraResources": [
      {
        "from": "data",
        "to": "data"
      }
    ]
  }
}